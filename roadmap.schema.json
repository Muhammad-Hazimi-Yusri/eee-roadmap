{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://eee-roadmap.muhammadhazimiyusri.uk/schemas/roadmap.schema.json",
  "title": "EEE Roadmap",
  "description": "Schema for EEE Roadmap track data files",
  "type": "object",
  "required": ["sections"],
  "properties": {
    "meta": {
      "$ref": "#/$defs/meta"
    },
    "sections": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/section"
      }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "meta": {
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "icon": { "type": "string" },
        "featured": { "type": "boolean" },
        "category": { "type": "string" },
        "order": { "type": "integer" }
      },
      "additionalProperties": false
    },
    "section": {
      "type": "object",
      "required": ["id", "title", "items"],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
        "title": { "type": "string" },
        "items": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/topic" }
        }
      },
      "additionalProperties": false
    },
    "topic": {
      "type": "object",
      "required": ["id", "title", "description"],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "prerequisites": { "type": "array", "items": { "type": "string" } },
        "outcomes": { "type": "array", "items": { "type": "string" } },
        "concepts": { "type": "array", "items": { "$ref": "#/$defs/concept" } },
        "resources": { "type": "array", "items": { "$ref": "#/$defs/resource" } },
        "optional": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "concept": {
      "oneOf": [
        {
          "description": "Inline concept with explicit name and optional notes",
          "type": "object",
          "required": ["name"],
          "properties": {
            "name": { "type": "string" },
            "notes": { "type": "string" }
          },
          "additionalProperties": false
        },
        {
          "description": "Library concept reference resolved at build time",
          "type": "object",
          "required": ["ref"],
          "properties": {
            "ref": { "type": "string", "pattern": "^[a-z0-9-]+$" },
            "override": {
              "type": "object",
              "properties": {
                "context_note": { "type": "string" },
                "notes_replace": { "type": "string" }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "resource": {
      "type": "object",
      "required": ["label", "url"],
      "properties": {
        "label": { "type": "string" },
        "url": { "type": "string", "format": "uri" }
      },
      "additionalProperties": false
    }
  }
}